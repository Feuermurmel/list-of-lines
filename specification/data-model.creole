= Data Model

This chapter will describe all concepts used by this specification and tries to explain how they fit together and rely on each other.


== Numbers

This specification makes use of two kinds of numbers to represent all numeric data.

**Natural numbers** are used to describe 


== Coordinates

//Coordinates// are used to specify the positions of //vertices//, so they can used to build up simplexes and later general polyhedra. Each coordinate is a tuple of `n` _rational numbers_.
